type TestimonialProps = {
    clientName: string;
    feedback: string;
    clientImage: string;
    rating: number;
}


const Testimonial = ({ clientName, feedback, clientImage, rating}: TestimonialProps) => {
    return (
        <div>

        </div>
    )
}

export default Testimonial;